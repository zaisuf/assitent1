{
  "name": "webstock-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "postinstall": "cd server && npm install --no-audit --no-fund"
  },
  "engines": {
    "node": ">=14"
  }
  ,
  "dependencies": {
    "@google-cloud/speech": "^5.0.0",
    "ws": "^8.13.0"
  }
}
