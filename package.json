{
  "name": "webstock-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "postinstall": "cd server && npm install --no-audit --no-fund"
  },
  "engines": {
    "node": ">=14"
  }
}
